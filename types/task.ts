export type Status = {
  uuid: string,
  name: string,
};

export type Task = {
    uuid: string;
    name: string;
    description: string;
    status: string;
}

/*export type Tasks = {
    status: string;
    tasks: Task[];
}*/